#include <stdio.h>
/**
 * main - print all hexadecimal numbers in lowercase
 *
 * retunr: zero
 */
int main(void)
{
	char hex;
	for(hex = 0; hex++)
	{
		putchar(hex);
	}
	putchar('\n')
	return (0);
}
