#include "main.h"

/**
  * abs - compute absolute value of an integer
  *
  * Return: the ablsolute value of the integer
  */

int _abs(int)
{
	int n;

	if (n >= 0)
		return (n);
	else
		return (-n);
}
